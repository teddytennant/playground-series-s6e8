w111 PRE-REGISTRATION — 2026-08-28, slot 10 of 10
ANGLE as handed: "Consolidation: no new ideas. Re-verify the best pipeline end-to-end, check the
CV-to-LB gap across every experiment so far, and make sure the strongest submission is the one
selected."

CONSTRAINT KNOWN BEFORE ANY WORK
--------------------------------
w26g_send.py --n 10: 171 submissions on record, 10 already sent 2026-08-28 (UTC), 0 of 10 slots
left. NOTHING SHIPS FROM THIS RUN. No member is built, no drop list is edited, the registered
08-29 queue is not re-litigated, the deadline pick does not move except by a HUMAN click.

STATUS OF THE ANGLE BEFORE ANY WORK
-----------------------------------
This is ANGLE INDEX row 10 (consolidation), handed x5 before today (08-11 s8, 08-13 s5,
08-16 w16c, 08-19 w28 s9, 08-24 w75 s4) and now x6. Row 10 is the ONLY row of the ten that is
not a closed modelling angle: it is the standing checklist, and RESEARCH says explicitly that
consolidation is where the remaining value is. So it is taken AS ISSUED and EXECUTED, not
refused with a citation. It is also the one row never re-verified at the artefact level
(rows 3,4,5,6,7,9 have been, by w105-w110).

THE THREE INSTRUMENTS ROW 10 NAMES, PLUS THE ONE THE ANGLE'S MIDDLE CLAUSE NAMES
--------------------------------------------------------------------------------
  I1  check_selection.py       is the strongest submission selected?      live, read-only
  I2  w93a_suite.py            45 standing checks                          ~255s
  I3  w93b_cvlbaudit.py        CV-to-LB gap over EVERY file sent           live, read-only
  I4  w84a_pickargmax + w93c_pickverify   the CV-argmax pick, and its verification

I3's artefact on disk is STALE: w93b_cvlbaudit.json was written at 160 sends and there are now
171. Re-running it is the angle's middle clause done properly, and it writes only its own file.

REGISTERED PREDICTIONS
----------------------
P1  I1 exits rc=1 with "NOTHING IS SELECTED" and names refs 55656399 and 55588167.
P2  I3 re-run live at 171 sends: gap_mean stays within 1e-4 of the published 0.0010358;
    gap_sd stays below 6e-5 (published 3.392e-5); pearson stays above 0.80 (published 0.8842).
P3  I3's cv_pair is UNCHANGED at (w36_ad199stdcorr.csv, w38_ad202stdcorr.csv) - none of the 11
    files sent since the audit was written displaces the CV argmax, because today's ten were
    pinned exploratory files (CV nan) or blends at CV 0.96995-0.96996, all below 0.9701400.
P4  I4 green, and the file pair it names is the same pair I1 names by ref.
P5  the full suite is 45/45 GREEN under systemd-run with PATH inherited - INCLUDING
    w54a_vetoexpiry and w85c_slotguard, which fail by design only when the queue on disk was
    written for the day just sent. The queue on disk is for 2026-08-29, the next UNSENT day,
    so the freshness condition is satisfied and neither should be red.

FALSIFICATION AND WHAT EACH MISS MEANS
--------------------------------------
P1 miss (rc=0): the click has been done - that is the largest open item closing, record it.
P2 miss: the CV-to-LB relation has moved with 11 more sends; report the new numbers as the
    current ones and say which direction it moved. Not a reason to change a pick.
P3 miss: a file sent since w93 outranks the deadline pick ON CV. That WOULD change the pick,
    and it is the one outcome of this run that could move a decision. Verify with I4 before
    believing it.
P4 miss: the selection instruments disagree with each other; diagnose before anything else.
P5 miss: read the FULL failure log at experiments/w93a_fail_<stem>.log before diagnosing, and
    treat a red under systemd-run as an ENVIRONMENT hypothesis until re-run in the shell (w110).

Read-only run. No file that any other module consumes is written except w93b_cvlbaudit.json,
which is its own and is read by nothing.

ADDENDUM (written after I1-I3 returned, BEFORE the reproduction below was launched)
====================================================================================
WHY THIS ADDENDUM. The angle's FIRST clause is "re-verify the best pipeline end-to-end", and
none of I1-I4 does that. w93c_pickverify's own docstring names the gap in one sentence: "the CV
is computed on the OOF vector; the file Kaggle scores is a DIFFERENT ARRAY written by the same
build, and nothing on disk has ever asserted that the shipped file is a well-formed submission
for THIS test set". w93c then asserts well-formedness (G3) but NOT provenance. So the last link
of the deadline chain -- shipped CSV <- same build as the OOF vector the CV comes from -- has
never been checked, and it is the link the whole selection argument rests on.

IT IS CHEAP TO CHECK, BECAUSE THE PICK IS NOT A TRAINED OBJECT. w36_ad199stdcorr is w21a
post-hoc rank arithmetic over the base w36_ad199std, whose OOF and CSV are both on disk
(w60a_run.sh; "no refit, no new members, no search"). w60a ran TWO w21a arms concurrently in
23 minutes, so one arm is ~20 min of numpy.

THE RUN. `W21A_BASE=w36_ad199std W21A_TAG=w111repro .venv/bin/python
experiments/w21a_ad187corr.py` -- the shipped recipe verbatim, only the TAG changed.

REGISTERED PREDICTIONS
  Q1  the reproduced OOF CV equals the pick's claimed 0.9701400060. This is rank arithmetic
      over a stored vector, not a solver, so I register EXACT to < 1e-9, NOT merely inside the
      3.68e-6 BLAS floor. A miss inside the floor is still a miss of Q1 as registered.
  Q2  the reproduced test CSV is RANK-IDENTICAL to submissions/w36_ad199stdcorr.csv. The
      script prints this itself ("rank-identical to an existing submission file?"), so the
      instrument is the shipped one and not something I wrote after seeing the answer.
      AUC reads only the ordering, so rank-identity is the exact statement "the file Kaggle
      will score is the artefact whose CV the selection is made on".
  Q3  the base's own CV reads 0.9701323250 and the correction is +4.263e-6 -- w60a_ad199_ens4
      names the base figure for the ens4 side; this is the h3-side twin and must agree with
      the ledger's w36_ad199std.
  Q4  no file already on the 171-file sent ledger changes. Verified by md5 over submissions/
      before and after, and the two scratch outputs are MOVED OUT of submissions/ the moment
      the run ends.

FALSIFICATION AND CONSEQUENCE
  Q1 miss: the claimed CV is not reproducible from the base -- report, and w93c's G1 (which
      recomputes from the STORED oof vector, a different route) becomes the only witness.
  Q2 miss: THE SHIPPED FILE IS NOT THE ARTEFACT ITS CV DESCRIBES. That is a hole in the
      deadline argument and would be the finding of this run. It does NOT license moving the
      pick on the spot -- it licenses saying so loudly and checking slot 2 the same way.
  Q3 miss: the base on disk is not the base the pick was built from.
  Q4 miss: STOP. Restore from git and write it at the top of the journal entry.

NOTHING SHIPS EITHER WAY. 0 of 10 slots. WANTED does not move: w64a settled slot 2 at
+0.1444e-6 and its two named re-open conditions are both unmet (slot 1 is still the strict CV
argmax of everything sent; no new pack ladder exists).

ADDENDUM 2 — MY BASE WAS WRONG, CAUGHT 3 MINUTES IN, AND THE FIX BUYS A SECOND ARM
==================================================================================
I read the base off `experiments/w60a_run.sh`, which sets W21A_BASE=w36_ad199std. That script
builds the **ens4 twin** (TAG w36_ad199stdcorr_ens4), not the pick. The pick's own artefact,
`experiments/w21a_w36_ad199stdcorr.json`, records base = **w36_ad199std_h3**, base_auc
0.9701354276. So the run launched at 16:47Z is reproducing w36_ad199stdcorr_ens4, not
w36_ad199stdcorr. Caught from the log's first line (base AUC 0.9701323250 = the ens4 base) two
arms in, BEFORE any result was read as evidence for anything.

⟹ same genus as w64's "a deferral copied forward copies its candidate forward too": I took the
base from a RECIPE THAT NAMES A SIBLING instead of from the artefact of the file itself.

THE FIX. Let the running arm finish -- it reproduces `w36_ad199stdcorr_ens4`, which is one of
the TWO files Kaggle's auto-selection would take, so it is worth having -- and launch the
correct arm concurrently, which is exactly what w60a itself did (two w21a arms, 23 min total,
"rank/AUC arithmetic over numpy, not a thread-hungry GBDT").

  arm A (running)  BASE w36_ad199std     TAG w111repro     -> expect w36_ad199stdcorr_ens4
  arm B (new)      BASE w36_ad199std_h3  TAG w111repro_h3  -> expect w36_ad199stdcorr  (THE PICK)

PREDICTIONS, RESTATED PER ARM (Q1/Q2/Q4 unchanged in form)
  A1  arm A base AUC 0.9701323250 -- ALREADY CONFIRMED in its first log line.
  A2  arm A ships CV 0.9701365875 and is rank-identical to w36_ad199stdcorr_ens4.csv.
  B1  arm B base AUC 0.9701354276 (the h3 base, from the pick's own artefact).
  B2  arm B ships CV 0.9701400060 and is rank-identical to w36_ad199stdcorr.csv -- THE PICK.
  Both: nested_delta and naive_delta reproduce (A: +2.232e-6 / +4.632e-6, scheme_optimism
  +2.400e-6; B: +5.1535e-6 / +5.1535e-6, scheme_optimism 0.0).

⚠ B2 is the load-bearing one. A is a bonus that happens to price the other side of the click:
if BOTH reproduce, then the CV pick and the file auto-selection would take are each verified to
be the artefact their CV describes, and the click argument rests on two checked objects.
