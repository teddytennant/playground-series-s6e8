{
  "server_page_cap": 200,
  "true_total": 236,
  "at_cap": 200,
  "over_cap": 200,
  "call_sites": 44,
  "paginated_files": 16,
  "g4_missing": [],
  "failures": [],
  "backlog_dormant": [
    "G2 w15j_cvlb.py:17 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w15j_lblaw.py:27 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w17a_cvlb_scatter.py:97 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w17b_famfix.py:69 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w17d_coupling.py:117 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w17e_logitpair.py:77 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w17f_readout.py:42 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w17g_parametric.py:68 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w17j_hybridpair.py:86 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w17k_readout.py:53 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w18a_lawif_joint.py:137 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w18b_rankraw.py:96 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w18d_readout.py:39 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w19a_transfer.py:129 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w19c_clicksens.py:58 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w19d_taupost.py:66 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w25a_cvlb_full.py:33 page-size 300 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 300` check can never fire",
    "G2 w25e_stdpairs.py:40 page-size 300 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 300` check can never fire",
    "G2 w28a_cvlb_refresh.py:65 page-size 300 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 300` check can never fire",
    "G2 w28b_verify.py:64 page-size 300 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 300` check can never fire",
    "G2 w30a_oos10.py:36 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w37e_readout.py:25 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w39c_gapaudit.py:77 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w45a_tierprice.py:72 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w46a_sendhazard.py:83 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w50b_autoselect.py:53 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w57a_tierprice2.py:78 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w58a_tiergate.py:87 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w59a_hijackprice.py:99 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w62a_autopair.py:205 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w63a_setprice.py:277 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w64a_hedgeprice.py:101 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w66a_pricercal.py:355 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w66a_pricercal.py:88 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w66b_leverage.py:53 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w67a_aboveslope.py:291 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w74a_clickprice.py:300 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w75a_erarefresh.py:135 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w76a_addtest.py:74 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire",
    "G2 w85b_prereg.py:75 page-size 500 is above the server cap 200; the read is capped at 200 and a `len(rows) >= 500` check can never fire"
  ]
}